# Snippet page micro-copy and form tweak

1. Update sub-headline to: "Your free Tikkun birth chart + PDF workbook" — color changed to gold (`C_GOLD`).
2. Add optional Name field above the Email field in the form (label: "Name (optional)", no `required` attribute).
